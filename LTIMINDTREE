

LTI mindtree
debouncing vs throtteeling
react use memo, use callback, use memo
TIme compnexity , of code 
react memo vs pre component 
HOC

TWo sum array with give target code and TC should be O(n)

function twoSum(nums, target) {
  const map = new Map(); // stores value -> index

  for (let i = 0; i < nums.length; i++) {
    let complement = target - nums[i];

    if (map.has(complement)) {
      return [map.get(complement), i]; // indices of the two numbers
    }

    map.set(nums[i], i);
  }

  return [];
}

// Example
console.log(twoSum([2, 7, 11, 15], 9)); // Output: [0, 1]
